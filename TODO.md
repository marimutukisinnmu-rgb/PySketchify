# TODO

## 現在の最優先

- [ ] 動画サイズ情報を `ffprobe` から取得する
  - `width`
  - `height`
  - `r_frame_rate`
- [ ] 取得した `width` / `height` / `r_frame_rate` を親プロセスから描画Workerへ明示的に渡す
- [ ] 各Workerが受け取った画面サイズをログへ出して確認できるようにする
- [x] Worker化後に発生していた映像の表示・フレーム境界問題を確認する
  - [x] FFmpeg → raw RGB24 の100 frame目を単体確認
  - [x] `data` サイズが `width × height × 3` と一致することを確認
  - [x] 受信したRGB24画像が正常に表示されることを確認
  - [x] 描画処理なしで `data` をそのまま扱った場合も正常であることを確認
  - [x] 描画処理を入れた場合にどの段階で画像が崩れるか切り分ける
  - [x] 原因を修正: 小さい解析キャンバスへフル解像度座標で描画していた

## 高速化

- [ ] 破片式・チャンク式の処理思想を維持しつつWorker並列化する
- [ ] range単位のWorker処理を安定化する
- [ ] RAM使用量とWorker数のバランスを調整する
- [ ] GPU使用時のWorker数を検証する

## 表示・進捗

- [x] 推測残り時間（ETA）を表示する

## 出力

- [ ] 入力動画と同じ解像度・FPSで出力されることを確認する
- [ ] アスペクト比・表示領域が崩れないことを確認する
- [ ] 音声など、必要なストリームの扱いを検討する

## 後回し

- [ ] README.md の整備
- [ ] CLI / GUI のバージョン表記を整理する
- [ ] エラー表示・ログを整理する
